'use strict';

const {
  DEFAULT_COUNT,
  FILE_NAME,
  TITLES,
  SENTENCES,
  CATEGORIES,
  OfferType,
  SumRestrict,
  PictureRestrict
} = require(`./const`);

module.exports = {
  name: `--generate`,
  run() {
  }
}
